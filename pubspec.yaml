name: smart_classroom_facilitator_project
description: "A Smart Classroom Management System with Adaptive Recitation."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  # Icons and Localization
  cupertino_icons: ^1.0.8
  intl: ^0.20.2

  # Backend Communication (Added for Node.js Integration)
  http: ^1.1.0

  # Utilities
  file_picker: ^10.3.10
  socket_io_client: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Ensure your assets are registered if you use them later
  # assets:
  #   - assets/images/